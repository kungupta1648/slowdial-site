# slowdial.app

The waitlist page's source, kept here so it has a permanent home outside a
temp session folder. This is the same page published as a Claude Artifact —
this copy is what you upload to real hosting (Cloudflare Pages) to serve it
from slowdial.app.

To update: edit index.html, then re-upload it in the Cloudflare Pages
dashboard (Workers & Pages → slowdial-app → Deployments → re-upload), or ask
Claude to hand you a fresh copy after a further edit.
